# Relation Management Android

Prototype Android wrapper for the OnlineImoti mobile relation management flow.

## Current scope

- local `Base URL` and `access token` settings
- test lookup request to `/relationship-manager/lookup.php`
- native notification with quick open action
- remote `WebView` form loading from `/relationship-manager/form.php`
