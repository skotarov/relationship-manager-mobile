# CallLog Android

Prototype Android wrapper for the OnlineImoti mobile call report flow.

## Current scope

- local `Base URL` and `access token` settings
- test lookup request to `/broker/callreport/lookup.php`
- native notification with quick open action
- remote `WebView` form loading from `/broker/callreport/form.php`
