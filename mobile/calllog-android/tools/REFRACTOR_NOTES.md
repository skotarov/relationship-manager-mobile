# Safe large-file refactor

This branch only extracts existing cohesive blocks into separate files. No storage format, queue semantics, call-state behavior, SMS pagination, translation fallback, endpoint, permission or UI text is intentionally changed.
